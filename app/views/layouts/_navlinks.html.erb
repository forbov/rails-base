<ul class="navbar-nav me-auto mb-2 mb-lg-0">
  <li class="nav-item">
    <%= link_to root_path, class: "nav-link active", aria: {current: "page"} do %>
      <span class="fi-home"></span> Home
    <% end %>
  </li>
  <% if user_signed_in? %>
    <li class="nav-item">
      <%= link_to users_path, class: "nav-link" do %>
        <span class="fi-torsos-all"></span> Users
      <% end %>
    </li>
  <% end %>
</ul>
<ul class="nav navbar-nav navbar-right">
  <li class="nav-item">
    <% if User.any? %>
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <span class="fi-torso"></span>&nbsp; <%= session[:user_email] %>
          </a>
          <ul class="dropdown-menu">
            <li>
              <%= link_to user_path(current_user), class: "dropdown-item" do %>
                <span class="fi-torso"></span> Profile
              <% end %>
            </li>
            <li><hr class="dropdown-divider"></li>
            <li>
              <%= link_to destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" do %>
                <span class="fi-eject"></span> Sign Out
              <% end %>
            </li>
          </ul>
        </li>
      <% else %>
        <%= link_to login_path, class: "nav-link" do %>
          <span class="fi-play"></span> Sign In
        <% end %>
      <% end %>
    <% else %>
      <%= link_to new_user_registration_path, class: "nav-link" do %>
        <span class="fi-torso"></span> Set Admininstrator
      <% end %>
    <% end %>
  </li>
</ul>
